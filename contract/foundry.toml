[profile.default]
src = "."
out = "out"
libs = ["lib"]
fs_permissions = [{ access = "read-write", path = "../js"},{ access = "read-write", path = "/tmp/"}]
ffi = true

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
